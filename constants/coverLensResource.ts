/**
 * URL base del servidor CoverLens Resource (VPS).
 * Campos de idiomas esperados en cada JSON de juego: textLanguages, voiceLanguages
 * (array de códigos ISO o nombres, p. ej. ["es","en"]).
 */
export const COVERLENS_RESOURCE_BASE_URL = 'https://covers.cholloweb.es';
